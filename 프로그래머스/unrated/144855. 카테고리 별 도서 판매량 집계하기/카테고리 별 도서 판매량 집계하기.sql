SELECT CATEGORY, SUM(SALES) AS TOTAL_SALES FROM BOOK AS b RIGHT JOIN BOOK_SALES AS bs ON b.BOOK_ID = bs.BOOK_ID WHERE YEAR(bs.SALES_DATE) = 2022 AND MONTH(bs.SALES_DATE) = 1 GROUP BY CATEGORY ORDER BY CATEGORY;